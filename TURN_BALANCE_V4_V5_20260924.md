# 2026-09-24：Open Duck Mini 转向续训 v4/v5

## 改动与训练

上轮诊断发现原域随机化把 `geom 0` 当作地面；实际平地 XML 中地面为
`geom 46`，左右脚底为 `18` 和 `43`。v4 按模型中的名称取得这些 ID，
给地面与两只脚底设置同一个随机滑动摩擦系数，范围为 `0.3–1.4`。
原先的奖励总值在每控制步被截为非负；v4 将下限设为 `-5`，
并对转向时双脚同时触地增加 `-2` 的惩罚。

v4 从 `official_seed_turn_balance_v2/2026_09_23_122903_5079040` 续训
2,621,440 步，4096 个并行环境，学习率 `2e-7`。完整参数见
`scripts/launch_turn_balance_v4.sh`。

v5 从 v4 最终检查点续训相同步数，学习率降为 `1e-7`；
双脚同时触地惩罚调为 `-5`，并增加 `-8 × |目标偏航角速度 - 实际偏航角速度| / |目标偏航角速度|`
（分母下限为 `0.05 rad/s`）。该项同时惩罚欠速和超速。完整参数见
`scripts/launch_turn_balance_v5.sh`。两轮的检查点和 ONNX 均保留在服务器，
没有覆盖 v2 或 v4。

## 连续动作验证

原生 MuJoCo 顺序执行站立、`0.03 m/s` 低速、停止、`0.1 m/s` 直走、
停止、`+0.15 rad/s` 转向、停止、`-0.15 rad/s` 转向、最终停止。
每个摩擦系数测试 3 个随机速度扰动种子，每档共 27 个阶段。
表内数值为 3 种子的平均值，转向持续 5 秒。目标转角绝对值约为 `42.97°`。

| 摩擦系数 | v4 完成/跌倒 | v4 直走 m/s | v4 正/反转角 | v5 完成/跌倒 | v5 直走 m/s | v5 正/反转角 |
| --- | --- | ---: | ---: | --- | ---: | ---: |
| 0.3 | 27/27，0 | 约 0.10 | +50.32° / -11.86° | 27/27，0 | 约 0.10 | +52.58° / -13.14° |
| 0.6 | 27/27，0 | 约 0.11 | +56.28° / -24.23° | 27/27，0 | 约 0.11 | +56.49° / -28.70° |
| 1.0 | 27/27，0 | 约 0.11 | +54.88° / -9.29° | 27/27，0 | 约 0.11 | +55.50° / -11.92° |
| 1.4 | 27/27，0 | 约 0.11 | +49.36° / -6.06° | 27/27，0 | 约 0.11 | +49.40° / -6.41° |

上述结果见 `results/contact_grid_v4_friction_*_20260924.json` 和
`results/contact_grid_v5_yaw_error_*_20260924.json`。
脚本 `validate_sim2real_policy.py --contact-friction` 在运行时同步设置
地面与两只脚底的滑动摩擦，未改动 XML。

## 独立站姿转向扫描

每次重新从标准站姿起步，站立 3 秒后单独转向 5 秒。
该扫描能隔离此前行走、停车对内部动作历史和身体姿态的影响。
四档摩擦从低到高，v4 的反向转角为
`-25.75°、-25.63°、-7.36°、-5.49°`；v5 为
`-13.97°、-27.43°、-8.39°、-5.87°`。因此 v5 在完整连续控制中
普遍略好，但在低摩擦的独立反向起步中明显弱于 v4。
高摩擦下两者反向转弯都严重欠速；v5 在摩擦 1.4 时反向双脚同时接触占比
仍为 99.2%。结果见 `results/turn_four_hypotheses_v4_20260924.json` 和
`results/turn_four_hypotheses_v5_20260924.json`。

为确认训练过程，v4 的 65 万、131 万和 197 万步检查点也分别导出并扫描。
摩擦 0.6 时反向转角从 `-10.04°`、`-13.73°`、`-17.97°`
持续改善到最终 `-25.63°`，说明 v4 对该场景并未在中途达到峰值。
v5 的相同检查点扫描表明低摩擦场景的反向退化并非仅发生在最后一步。
原始数据保存在 `results/turn_v4_trend_*_20260924.json` 和
`results/turn_v5_trend_*_20260924.json`。

## 保留的模型与限制

- v4 ONNX：`/data/shijinsheng/open_duck/training/official_seed_turn_balance_v4_friction/final.onnx`；SHA-256：`b01ba3d7055cd64b484b81f0661d6cfedaddf21a4aee8311e901f929809a3b0d`。
- v5 ONNX：`/data/shijinsheng/open_duck/training/official_seed_turn_balance_v5_yaw_error/final.onnx`；SHA-256：`6f60d1d0a0d5e9f780e18a0c202c816c8d0183c855f78fa0a2580dce776d383d`。

v5 是连续动作仿真中的较优候选，v4 保留作低摩擦独立转向对照。
两者都没有解决高摩擦反向转弯欠速，也没有经过实体机器人上的舵机零位、
IMU 坐标、供电和安全限幅验收。当前结论仅适用于本次仿真模型与测试条件。
