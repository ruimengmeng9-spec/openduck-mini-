# 2026-09-25：Open Duck Mini v2 负转向诊断与续训

## 测试条件

所有表中的运动评估均使用原始平地 XML，不在评估时改动摩擦：地面滑动摩擦系数为 `0.6`，左右脚底各为 `1.0`。独立起转测试从标准站姿站立 3 秒后执行 5 秒转向；连续动作测试为站立、低速行走、停车、直行、停车、正转、停车、负转、停车，使用 3 个初始速度扰动种子，共 27 个阶段。转向命令为 `±0.15 rad/s`，5 秒目标转角为 `±42.97°`。这里的“完成”只表示没有触发跌倒，并不等于速度跟踪达标。

训练使用原先的 v5 PPO 检查点和 4096 个并行环境。不同训练变体各自保存，不覆盖 v5。除特意标注外，训练保留原有动力学随机化；训练中的摩擦随机化是为覆盖模型不确定性，并不代表真实地面摩擦会随时间变化。所有验收均固定在原始 XML。

## 观察到的原因线索

- 参考步态的偏航速度档位不对称。在训练范围内，`-0.15 rad/s` 就近取到约 `-0.074` 的参考步态，`+0.15 rad/s` 则取到约 `+0.185`。模仿奖励中关节姿态误差的系数为 `15`，可能把弱负转步态作为较强先验。
- 进一步逐相位核对后确认：`(vx=0, yaw=-0.15)` 所取参考的 **10 个腿部关节位置与原地站立参考逐值相同**，腿部 RMSE 为 `0`；同样比较正转参考与站立的腿部 RMSE 为 `0.0213 rad`。参考帧记录的平均偏航角速度分别约为负转 `-0.000038 rad/s`、正转 `+0.00127 rad/s`，均远小于实际命令。这比“参考档位不对称”更直接地表明负转模仿目标本身基本是站立。
- v5 的原始摩擦独立扫速呈非单调：`-0.10/-0.15/-0.20/-0.25/-0.30 rad/s` 分别转过约 `-6.0°/-27.4°/-15.7°/-49.5°/-78.5°`。`-0.20` 比 `-0.15` 更弱，而跨过参考档位边界后又大幅增强。因此“给更大的负转指令”不能当作已训练好低速负转。
- v5 在 `-0.15` 独立转向时双脚同时触地约 `90.8%`，正转约 `82.8%`；负转力矩偏弱。舵机目标限速在该负转测试中未触发，不能把欠速简单归咎于电机速度上限。
- 先前测试的半周期相位翻转使 v5 连续负转从约 `-28.7°` 降到 `-9.8°`，故没有采用该运行时补偿。较早模型上的参考插值和完全移除转向模仿也未解决负转不足。

## 已完成的训练对照

| 模型 / 变体 | 与 v5 相比的主要变化 | 独立正转 / 负转 | 连续正转 / 负转，3 种子均值 | 直行均速 | 完成 / 跌倒 |
| --- | --- | ---: | ---: | ---: | ---: |
| v5 基线 | 无 | `+55.84° / -27.43°` | `+56.49° / -28.70°` | `0.11 m/s` | `27/27 / 0` |
| 固定物理、关闭全部域随机化，对照 | 额外续训 262 万步 | `+55.63° / -12.04°` | `+54.98° / -15.74°` | `0.11 m/s` | `27/27 / 0` |
| 固定物理、关闭全部域随机化，负转模仿 ×0.25 | 同上，仅改变负转模仿 | `+56.41° / -11.87°` | `+54.33° / -15.55°` | `0.11 m/s` | `27/27 / 0` |
| 保留原域随机化，配对对照 | 同 v5 训练设置，继续 262 万步 | `+55.77° / -27.98°` | `+56.41° / -24.26°` | `0.11 m/s` | `27/27 / 0` |
| 保留原域随机化，负转模仿 ×0.25 | 仅改变负转模仿 | `+55.51° / -15.98°` | `+56.99° / -30.55°` | `0.12 m/s` | `27/27 / 0` |
| 只将负转关节姿态模仿 ×0.25 | 保留接触和速度模仿 | `+54.97° / -25.08°` | `+57.63° / -30.62°` | `0.11 m/s` | `27/27 / 0` |
| 负转参考偏航指令 ×1.6 | 奖励和实际指令不变 | `+56.64° / -26.36°` | `+57.83° / -30.08°` | `0.11 m/s` | `27/27 / 0` |
| 扩大负转训练指令到 `0.13–0.30` | 负转样本占比提高，继续 524 万步 | `+54.44° / -11.77°` | `+54.13° / -12.56°` | `0.12 m/s` | `27/27 / 0` |
| 固定原始接触摩擦，负转集中在 `0.145–0.155` | 保留其他动力学随机化，继续 524 万步 | `+45.05° / -19.42°` | `+46.77° / -24.74°` | `0.12 m/s` | `27/27 / 0` |
| 上一行对照：负转双脚支撑惩罚 ×5 | 其余相同，继续 524 万步 | `+46.62° / -19.74°` | `+45.12° / -23.93°` | `0.12 m/s` | `27/27 / 0` |
| 固定原始接触摩擦，仅取消负转参考模仿 | 保留任务奖励与正转模仿，继续 524 万步 | `+43.77° / -18.37°` | `+46.19° / -23.94°` | `0.12 m/s` | `27/27 / 0` |

全部新模型在标准转向命令下都没有达到约 `-42.97°` 的目标；不能替换 v5。尤其是“降低整体模仿”虽然在连续动作里略有改善，却使独立起转大幅变弱，说明单一平均数会误导模型选择。扩大负转训练范围后，模型在 `-0.25/-0.30` 指令下可以转过 `-62.5°/-89.9°`，但在 `-0.15` 附近形成明显低速死区。随后针对 `-0.145` 至 `-0.155 rad/s` 的训练改善了正转精度，但没有解决负转欠速。

相关原始结果在 `results/turn_reference_ab_*.json`、`results/turn_v5_native_command_sweep.json` 和 `results/turn_negative_curriculum_sweep.json`，复现实验脚本为 `scripts/launch_turn_reference_ab.sh`。模型 ONNX 与重启检查点保存在服务器 `/data/shijinsheng/open_duck/training/turn_reference_ab_*`；不上传大检查点。

## 固定接触摩擦的下一轮

`targeted_negative` 分支仍随机化质量、惯量、关节摩擦和驱动参数，但通过 `RANDOMIZE_CONTACT_FRICTION=0` 保持 XML 中地面 `0.6`、脚底 `1.0` 不变。运行前用 2 个随机实例确认两者接触摩擦均保持 `[0.6, 1.0, 1.0]`，同时机身质量确实随机变化。该分支将负转训练集中在 `0.145–0.155 rad/s`，转向模式提高到 `60%`，并使用较强的负向参考步态。配对的 `targeted_support` 分支在此基础上仅将负转时双脚同时触地的惩罚乘以 `5`，用于验证接触锁定假设。

加强双脚支撑惩罚后，独立负转的双脚同时着地比例仅从约 `90%` 降到 `89%`，负转角没有改善。不能据此认为单纯扩大该惩罚会教出有效负转。

v5 的额外 JAX 奖励诊断（无观测噪声）表明，负转时约 `29.4%` 的样本瞬时角速度指向错误方向，正转约为 `5.5%`；但负转只有约 `0.5%` 的控制步低于训练奖励下限 `-5`。因此奖励下限截断不是这轮负转欠速的主因。原始数据见 `results/turn_reward_clipping_v5_fixed_20260925.json`。

## 参考动作可执行性

将参考轨迹直接送入普通 MuJoCo、以真实的 50 Hz 控制节拍和电机目标限速回放，得到以下结果：

| 控制方式 | 正转参考 `+0.185` | 负转参考 `-0.074` | 较快负转参考 `-0.333` |
| --- | ---: | ---: | ---: |
| 策略动作先限制在 `[-1,1]`，再限速 | `-3.3°`，未跌倒 | `+5.4°`，未跌倒 | `+8.8°`，未跌倒 |
| 不限动作幅度，只限电机目标变化速度 | `+42.6°`，未跌倒 | 约 `1.74 s` 跌倒 | 约 `1.66 s` 跌倒 |
| 参考关节位置直接给舵机目标 | 约 `1.6–1.7 s` 跌倒 | 约 `1.6–1.7 s` 跌倒 | 约 `1.6–1.7 s` 跌倒 |

因此负转参考并不是一个能直接交给当前可执行策略的安全教师：限制动作时不能有效负转，放开限制则失稳。上述测试是开环参考回放，不等同于学习策略的闭环表现。参考动作投影到策略空间时，头/颈关节始终超界，而正负参考的部分膝、髋关节都在少数相位超界；并非只有负参考出现简单的“动作被裁剪”现象。代码和原始结果分别见 `diagnostics/test_turn_reference_replay.py`、`diagnostics/check_turn_reference_action_bounds.py` 与 `results/turn_reference_replay_*_20260925.json`、`results/turn_reference_action_bounds_20260925.json`。

已运行 `scripts/launch_turn_reference_ab.sh negative_no_imitation`：在固定接触摩擦、负转命令集中采样的条件下，只取消负转的参考模仿奖励，保留任务奖励和正转模仿。独立负转 `-18.37°`，连续负转 `-23.94°`，均弱于 v5；虽然 `27/27` 阶段未跌倒，但该策略不能替换基线。这说明错误的参考先验并不是唯一限制：简单删去模仿约束并不会自动形成有效的负转动作。原始结果在 `results/turn_reference_ab_negative_no_imitation_*.json`。

## 镜像策略诊断与蒸馏

按照 MJCF 关节轴方向左右镜像观测和动作，用 v5 的正转行为构造负转教师。独立起转时，原策略负转 `-27.43°`；镜像策略负转 `-56.75°`，机身保持直立。将两者动作按权重混合，并仅在负转命令 `-0.15 rad/s` 使用，权重 `0.87` 的诊断控制器在 20 个随机初速的连续测试中完成 `180/180` 阶段、无跌倒，负转平均 `-43.30°`（范围 `-44.51°` 至 `-42.47°`），接近 5 秒目标 `-42.97°`。其他动作沿用原 v5。混合响应并非线性，例如权重 `0.50` 约为 `-0.88°`，因此不能仅凭两端结果插值。代码见 `diagnostics/test_turn_mirror_policy.py` 和 `validate_sim2real_policy.py`，原始结果见 `results/turn_mirror_policy_v5_20260925.json`、`results/turn_v5_mirror_blend*_20260925.json`。这仍是仿真诊断控制器，不是独立训练出的策略，也没有经过真机验证。

为使模型本身学习该动作，采集镜像教师在 24 个种子、4 种起转前缀下的 6000 条观测与动作，训练 115 维输入、14 维输出的两层 128 单元残差网络；残差只在 `[-0.16,-0.14] rad/s` 的负转命令启用，其他动作仍用原 v5。第一轮虽然留出样本离线误差较小，5 种子闭环却负转 `-46.96°` 至 `-62.36°`，说明存在分布偏移。随后将学生自身轨迹交给教师标注，进行了两轮 DAgger 式数据回流，每轮各 6000 条。第三轮残差模型在从未参加采集和训练的种子 `100–119` 上完成 `180/180` 阶段、无跌倒，负转平均 `-40.31°`（范围 `-41.29°` 至 `-39.18°`）；正转与直走维持 v5 的原有行为。与 v5 的约 `-29°` 相比明显改善，但仍低于目标。对应代码为 `diagnostics/collect_negative_mirror_teacher.py`、`scripts/train_negative_residual.py`、`diagnostics/negative_turn_residual.py`，结果见 `results/negative_mirror_distill_*.json`。

模型文件保存在服务器 `/data/shijinsheng/open_duck/training/negative_mirror_distill_v3/residual.onnx`，需与原 v5 ONNX 一起使用。v3 残差 SHA-256 为 `ca6a9073f7885a02fbe7ca288c5668beb8a02063580103a36488ea2baa532ce4`，v5 ONNX 为 `6f60d1d0a0d5e9f780e18a0c202c816c8d0183c855f78fa0a2580dce776d383d`。这些是实验模型，不替换稳定 v5，也不直接连接舵机。

## 结论边界

截至本轮 PPO 对照，负转不足与离散参考步态、双脚接触锁定和训练指令覆盖有关，但尚不能断言其中任意一项是唯一根因。后续镜像教师残差蒸馏在固定平地仿真的 `-0.13` 至 `-0.17 rad/s` 区间取得改善；训练和留出种子评估见 `NEGATIVE_TURN_DISTILL_20260926.md`。该实验没有在实体鸭子上验证，不替换硬件控制器；真机使用前仍需舵机限幅、急停与人工看护。
