# Open Duck Mini v2 后退跌倒：原因定位与对照测试（2026-09-26）

范围：原生 MuJoCo 平地仿真，v26 后退模型，指令 −0.074 m/s，50 Hz 控制、500 Hz 物理仿真。每组沿用相同 5 个初始速度扰动种子。没有在真机上运行这些策略，也没有修改摩擦系数或覆盖模型。

## 结论

最有力的解释是：**v26 会驱动机身后移，却没有及时把支撑脚移到足够靠后的落脚位置，也没有形成足够的俯仰恢复动作。重心逐渐接近并越过后侧支撑边界，随后俯仰和后退速度失控。** 这是一项由仿真轨迹和对照试验支持的机制判断，不是已经从训练代码中证明了唯一根因。

不宜把末段速度过高当作独立根因：在典型回合中，后侧支撑裕量减小、俯仰角持续恶化发生在速度突破 −0.15 m/s 之前。足底接触丢失更多发生在后续跌倒阶段。电机目标限速偶有触发，但跌倒前半秒平均裁剪接近零，关闭该限速也没有使策略稳定。

## 证据

1. **失败方向一致。** v26 在 −0.074 m/s 下 5/5 跌倒，持续时间为 2.54–3.92 秒。跌倒前半秒，平均局部前后速度为 −0.271 至 −0.308 m/s，远超过 −0.074 m/s 指令；俯仰角绝对值的峰值为 60.6–65.7°，横滚角峰值仅 6.9–7.7°。同一窗口内，电机目标限速的平均裁剪为 0–0.0012 rad/控制步，关节对目标的平均误差约为 0.045–0.054 rad。
2. **脚没跟上机身后移。** 以种子 0 为例，2.4–2.9 秒间机身沿初始前向轴从 −0.102 m 移到 −0.137 m；两只脚在世界坐标中的约略位置分别保持在 −0.067 至 −0.070 m、−0.110 至 −0.112 m。俯仰角从 −15.9° 发展至 −28.3°。采用模型 `base` 子树质心与足底接触点计算的后侧支撑裕量，在跌倒前半秒有 9–14/26 个控制采样点低于 −2 cm；首秒 5 个种子均为 0/50。种子 0 在首秒后 105 个采样中有 84 个仍为双脚接触、没有双脚同时腾空，故不是一开始就因完全失去接触而摔倒。支撑裕量只是动态步态的诊断指标，不能单独定义“必然跌倒”。
3. **指令幅度存在陡峭边界。** 指令 −0.03 m/s 时 5/5 完成 10 秒，但平均实际速度只有 −0.005 m/s；−0.04 时 4/5 完成、平均 −0.013 m/s；−0.05 时仅 1/5 完成、记录到的平均速度约 −0.052 m/s；−0.06 时 0/5 完成。模型从“基本原地站立”切换到“后退并失稳”，而不是平滑地产生可控低速后退。
4. **常见单项修补没有解决。** −0.074 m/s 基线 0/5、平均持续 3.16 秒；重置步态相位 0/5、1.72 秒；取消电机目标限速 0/5、3.70 秒；直接从 home 姿态启动 0/5、3.09 秒。三个按俯仰角减小后退策略权重的诊断性保护设置也均为 0/5，平均持续 1.82–2.99 秒。此前固定混合权重 ≤0.90 可以站稳，却仅以约 −0.003 m/s 后退；权重 ≥0.92 又重新跌倒。这说明简单调小动作或在俯仰过大时临时切换策略，不足以得到有效步态。

## 下一步实验方向

针对**后退落脚与俯仰恢复**重新设计训练，而不是继续提高速度奖励：先把足底接触、足端相对重心位置、俯仰角速度和速度跟踪分别作为可观测评估量；在训练中加入不依赖特权信息的可执行反馈，并建立“10 秒不跌倒且实际后退速度达到目标一半以上”的独立验收条件。必须在多个初始姿态、机身质量变化和真实电机目标限速条件下复测，达标前不接入真机。

复现脚本：[`diagnostics/diagnose_backward_fall.py`](diagnostics/diagnose_backward_fall.py)、[`validate_backward_sustained.py`](validate_backward_sustained.py)。核心数据见 `results/backward_cause_v26_*.json`、`results/backward_fall_trace_*.json`、`results/backward_fall_com_trajectory_v26_seed0_20260926.json`、`results/backward_pitch_guard_*.json`。
