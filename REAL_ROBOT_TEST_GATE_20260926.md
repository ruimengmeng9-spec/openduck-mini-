# Open Duck Mini v2：首次受控真机测试门槛（2026-09-26）

## 已知硬件状态

依据 `HARDWARE_VALIDATION_20260923.md`：树莓派运行官方 Runtime，14 个舵机及 IMU、左右足底接触传感器可以通信；策略为 50 Hz 位置控制，保留工厂零位和 `imu_upside_down=true`。这些记录证明基础链路已检查，**不证明当前学习策略能在地面安全行走**。

## 当前策略状态

- 基础 v5 是 101 维观测到 14 维动作的完整 ONNX 策略。
- 负转 v6 是额外的 115 维输入（101 维观测＋基础动作）到 14 维动作残差，需与 v5 串联；它不是可以直接替换官方 `--onnx_model_path` 的单个模型。
- v6 已在原始平地仿真的 `-0.13` 至 `-0.17 rad/s` 区间验证，区间外存在转向减弱。正在训练扩大低速范围的候选；未完成留出种子评估前不能升级。
- 目前没有在树莓派上做串联模型的 50 Hz 实时性、观测一致性或硬件动作验证。

## 接入舵机前必须通过的关卡

1. **软件离线回放**：从树莓派真实 Runtime 采集一段 101 维观测，不发送任何新电机目标；核对关节顺序、单位、坐标方向、站姿零位、动作缩放和历史动作更新。输入/输出必须是有限数，形状严格为 101/14。
2. **运行时安全层**：负转指令只允许经过验证的范围；对最终 14 个电机目标强制执行每周期位置和速度限幅，任何推理超时、非有限值、IMU/舵机失联或异常姿态都进入安全停止。不能仅依赖模型输出裁剪。
3. **实时性**：在目标树莓派上实测串联推理和整条 50 Hz 控制循环的延时分布及连续运行情况；服务器 CPU 的速度不能代替树莓派结果。
4. **悬空台架**：断开地面接触、有人看护且急停可随时触达，先检查归中和每个关节方向，再以短时、低增益命令检查站立、正负转动作。若关节打限位、振荡、方向不符或通讯异常，立即停止。
5. **吊架下接地**：仅在前面四项合格、供电和机械结构检查合格后，逐步做有人看护的短时接地测试。首次不能直接进行无人保护的连续行走。

2026-09-26 查看服务器当前官方 `scripts/v2_rl_walk_mujoco.py` 时，电机目标的 `max_motor_velocity` 限幅代码块处于注释状态。接新策略前必须确认实际运行文件的限幅路径并测试生效；不能把文件中的限幅常量存在当作限幅已生效。

因此仿真验收另需进行一次“不启用电机目标限速”的诊断对照（`validate_sim2real_policy.py --no-motor-slew-limit`），查明当前安全限速仿真和实际运行文件的差异。该对照**不是**建议真机关闭限速；真机应先实现并验证安全限幅。

本文件是**待完成的验收清单，不是部署批准**。官方 Runtime 的 [README](https://github.com/apirrone/Open_Duck_Mini_Runtime/blob/v2/README.md) 也将 IMU、电机和关节偏置检查置于运行行走策略之前。
