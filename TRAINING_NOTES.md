# 训练记录

## 已验证基线

- 仿真：MuJoCo/MJX，物理频率 500 Hz，控制频率 50 Hz。
- 策略接口：101 维观测，14 维连续动作。
- 标准站姿下，4 个随机种子均完成 30 秒行走。
- 站立 → 前进 → 停止 → 重新起步的连续测试全部完成。
- 普通 MuJoCo 与 JAX/MJX 都复现了转向不对称：正向转动明显快于负向。

## 后退专项实验

训练采用逐步课程，而不是直接覆盖已部署模型：

1. 早期版本学会明显后退，但会在 2–4 秒后倾倒；
2. 加入姿态、超速和模仿约束后能够保持稳定，但退化到接近站立；
3. 对齐训练与运行时的终止条件：训练提前在 `up_z < 0.75` 或机身高度 `< 0.10 m` 时终止；
4. 发现上游环境会把总奖励裁剪到 0，导致速度不足和方向错误的惩罚失效；
5. V11 允许后退任务获得负奖励，15 个原生 MuJoCo 验证组合均完成 5 秒，但速度只有约 `-0.006 m/s`；
6. V12 使用归一化进度、低速指令课程、更强倾倒惩罚，并从已产生后退动作的 checkpoint 恢复；它在 `-0.04 m/s` 指令下达到约 `-0.056` 到 `-0.070 m/s`，但 2.5–3.2 秒后倾倒；
7. V13 从 V12 的 1065 万步节点恢复，缩窄到 `-0.025` 至 `-0.035 m/s`，收紧超速阈值，并加入更强的倾角、机身高度和终止惩罚。

## 为什么不直接部署最后一步

PPO 的最终 checkpoint 不一定最好。每轮训练都保存多个 Orbax checkpoint，分别导出 ONNX 后，用普通 MuJoCo 运行时比较：

- 实际持续时间；
- 局部前向速度和位移；
- `up_vector_z` 与跌倒标志；
- 不同速度和随机种子的稳定性。

候选模型只有在这些指标通过后才可以复制到控制目录。

## 未解决问题

- 负向转向响应弱于正向；
- 后退策略尚未达到目标速度与稳定性的同时满足；
- 训练完成后仍需扩大到扰动、地面摩擦变化和多随机种子测试；
- 仿真策略还没有完成实体机器人上的舵机标定与安全限幅验证。
