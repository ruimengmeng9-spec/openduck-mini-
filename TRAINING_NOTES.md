# 训练记录

## 已验证基线

- 仿真：MuJoCo/MJX，物理频率 500 Hz，控制频率 50 Hz。
- 策略接口：101 维观测，14 维连续动作。
- 标准站姿下，4 个随机种子均完成 30 秒行走。
- 站立 → 前进 → 停止 → 重新起步的连续测试全部完成。
- 普通 MuJoCo 与 JAX/MJX 都复现了转向不对称：正向转动明显快于负向。

## 后退专项实验

训练采用逐步课程，而不是直接覆盖已部署模型：

1. 早期版本学会明显后退，但会在 2–4 秒后倾倒；
2. 加入姿态、超速和模仿约束后能够保持稳定，但退化到接近站立；
3. 对齐训练与运行时的终止条件：训练提前在 `up_z < 0.75` 或机身高度 `< 0.10 m` 时终止；
4. 发现上游环境会把总奖励裁剪到 0，导致速度不足和方向错误的惩罚失效；
5. V11 允许后退任务获得负奖励，15 个原生 MuJoCo 验证组合均完成 5 秒，但速度只有约 `-0.006 m/s`；
6. V12 使用归一化进度、低速指令课程、更强倾倒惩罚，并从已产生后退动作的 checkpoint 恢复；它在 `-0.04 m/s` 指令下达到约 `-0.056` 到 `-0.070 m/s`，但 2.5–3.2 秒后倾倒；
7. V13 从 V12 的 1065 万步节点恢复，缩窄到 `-0.025` 至 `-0.035 m/s`，收紧超速阈值，并加入更强的倾角、机身高度和终止惩罚。

## 转向专项实验

基线中已记录「负向转向明显弱于正向」。V1 转向策略在 `-0.3 rad/s` 指令下只得到约
`-0.127 rad/s`，正向约为 `0.125 rad/s`，说明弱项来自响应幅度而不是方向识别。

Turn V2 使用 `focused_skill.py` 的 `turn` 课程，从 V1 训练的第 1 亿步 checkpoint
（`walk_turn_stop_v1_formal_20260911_162747/2026_09_11_164237_100270080`）恢复，
在单张 GPU 上继续训练 1200 万步（1024 并行环境、学习率 `3e-5`、seed 57）。
与后退专项不同，转向任务保留正奖励裁剪、较松的姿态约束和 `0.85` 的移动指令比例，
因为平衡本身不再是瓶颈。

原生 MuJoCo 对比（5 秒、每个方向各一组，`up_vector_z` 均大于 0.99、无跌倒）：

| 指令 (rad/s) | V1 实测 (rad/s) | V2 实测 (rad/s) |
| --- | --- | --- |
| `+0.30` | 0.125 | 0.167 |
| `-0.30` | -0.127 | -0.207 |
| `+0.15` | 0.101 | 0.115 |
| `-0.15` | -0.083 | -0.141 |

结论：V2 把负向响应从 `-0.127` 提升到 `-0.207 rad/s`（约 +63%），正向同时提升，
且四组测试全部保持直立。负向已不再弱于正向，但两个方向都仍低于指令值，说明
超越 `0.3 rad/s` 的指令需要专门的高转速课程，而不是继续微调当前配置。

复现命令：

```bash
cd "$OPEN_DUCK_ROOT/projects/Open_Duck_Playground"
CUDA_VISIBLE_DEVICES="" JAX_PLATFORMS=cpu OMP_NUM_THREADS=1 \
  .venv/bin/python -u validate_turn_v2.py
```

## 后退退化：真正的根因是「指令量化」与「超速带宽」

先纠正一个曾经的错误结论：**参考动作数据集是有后退步态的**。网格为
`dx ∈ {-0.148, -0.074, 0.0, +0.074, +0.148, +0.222}`，其中 `-0.074` 条目与 `+0.074`
条目是**不同的步态**（系数最大绝对差 8471）。先前"负速度参考与正速度相同"的说法来自
只比较了 `±0.05`——它们恰好都四舍五入到 `±0.074` 这一对镜像条目，向量范数因步态对称而
接近（17.9686），才造成了误判。

真正的根因有两个：

1. **指令量化把低速后退指令映射到了「站立参考」。** 网格步长为 ±0.074，因此
   `-0.03` 指令经 `vel_to_index` 取最近邻后**精确落在 `dx = 0.000`**（实测
   `max|低指令参考 − 站立参考| = 0.0000`）。v13 的课程 `-0.025 ~ -0.035` 全部落在这一条，
   配上 `imitation = 0.25` 就把策略**主动锚死在站立姿态**。V15 把 imitation 置零之所以
   有效，正是因为它移除了这个「站立锚」，而不是因为负速度没有参考。
2. **超速惩罚的允许带宽比步态速度波动还窄。** `overspeed = max(|v_xy| − (1.10·|v_cmd| + 0.005), 0)`
   配 `-4000` 权重。`-0.04` 指令下允许速度仅 0.049 m/s，低于行走步态的正常瞬时波动，
   于是「移动」本身每步被罚约 −8，而进度奖励只值约 +2.5，「站着不动」严格更优。
   这解释了 v12–v14 每轮收紧惩罚都收敛到静止。

### V15 修复与实测

`imitation` 对后退置 0，超速带宽改为 `max(1.5·|v_cmd|, 0.15)`、权重降到 600。
`diagnostics/diag_backward_reward.py` 的算术核对显示激励方向被反转：

| | 移动（每步） | 站立（每步） | 差值 |
| --- | --- | --- | --- |
| 修复前 | −4.080 | −4.328 | 0.248（噪声量级） |
| 修复后 | **+3.200** | −0.248 | **3.448** |

原生 MuJoCo 实测：

| 候选 | 指令 −0.04 | 指令 −0.06 | 跌倒 |
| --- | --- | --- | --- |
| V12 | −0.069 | −0.097 | 是（1.9–2.5 秒） |
| V13 | −0.004 | −0.006 | 否（等于没动） |
| V14 | −0.006 | −0.007 | 否（等于没动） |
| V15 | −0.072 | −0.095 | 是（1.8–2.5 秒） |
| V16（提高姿态惩罚） | −0.066 | −0.090 | 是（1.9–2.6 秒） |

**结论**：后退速度恢复到 V12 水平（约 −0.07 ~ −0.10 m/s），但**仍会在约 2 秒后跌倒**。
V16 单纯提高姿态/终止惩罚没有解决它——因为 v15 已经把 imitation 关掉，策略失去了步态
先验，只能「猛冲后倒」。

### V17–V19：参考步态是稳定的，但策略始终跟不住

**关键测量**：把参考轨迹当关节目标**开环回放** 5 秒，每个扰动档位各 5 个随机种子
（`diagnostics/test_ref_robustness.py`）：

| 参考 | 无扰动 | 扰动 1e-4 | 扰动 0.05 | 结论 |
| --- | --- | --- | --- | --- |
| **后退 −0.074** | 0/5 跌倒 | 0/5 | **0/5** | **鲁棒稳定** |
| 前进 +0.074 | 5/5 跌倒（1.5–2.2 秒） | 5/5 | 5/5 | 开环不稳定（官方 ONNX 是反馈控制器） |

也就是说，**数据集里存在一条经得起扰动的稳定后退步态**（振幅检查
`check_ref_amplitude.py` 确认它有真实迈步动作，每周期每关节幅度约 0.28 rad，
不是站姿）。随后 V17/V18/V19 把课程对准该网格值 `−0.074`，并把模仿权重从 0
一路扫到 8 和 20，同时去掉零指令混合：

| 候选 | 指令 −0.074 实测 | 跌倒 |
| --- | --- | --- |
| V17（imit 0.5 / 1.0） | −0.105 / −0.112 | 是（1.6–1.7 秒） |
| V18（imit 8 / 20） | −0.110 / −0.107 | 是（1.6–1.7 秒） |
| V19（纯负指令，imit 8 / 20） | −0.100 / −0.099 | 是（1.7 秒） |

后退速度已优于任何历史版本（最快 −0.112 m/s，V12 为 −0.097），但**稳定性始终没有出现**。

已排除的原因：奖励算术（v15 已修）、模仿权重（0→20 全覆盖）、指令量化（已对准网格）、
零指令混合（v19 已去除）、姿态惩罚（v16 已提高）、物理被改动（官方行走 ONNX 在新旧场景
表现完全一致）、JAX 与 MuJoCo 不一致（策略在**自己的 JAX 训练环境**里同样在 1.48 秒
`done=1.0` 终止）。

**结论**：瓶颈在于 PPO 学不会跟踪这条稳定参考。即使模仿权重加到 20，关节跟踪惩罚
（约 −5.4/步）已远大于进度奖励（约 +1.6/步），跟踪行为在 1200 万步内仍未涌现。
已核对的项：参考向量确为 40 维（`diagnostics/check_ref_dims.py`），模仿奖励的
关节位置/速度、足端接触、机身线速度/角速度五个分项都正常生效，不存在空切片；
观测中也确实包含 `imitation_phase`，周期性参考是相位可观测的。

方向：①训练步数需大幅增加（行走策略用了 1.5 亿步，此处仅 1200 万，长训练进行中）；
②可考虑两阶段——先固定指令纯模仿该参考，再微调速度跟踪。

## 爬起（getup）专项
## 爬起（getup）专项

前置障碍：上游 XML 只有两只脚底有碰撞体，机器人躺倒后会穿过地面（机身高度实测降到
`-0.169 m`），「倒地」状态在物理上不存在。已由 `make_getup_model.py` 派生带躯干/头部
碰撞盒的副本（不改原文件），并在 `verify_getup_model.py` 中验证：原模型仍是 3 个碰撞
geom、新模型多出 `trunk_collision`/`head_collision`、90° 倾倒姿态空跑 4000 步后
`base z = 0.097`（停在地板上）。

环境侧改动：`reset()` 从倒地姿态起始（倾斜 80–105°、随机朝向、四种卧姿）；
关闭「翻倒即终止」；`stand_up = upright² × clip(高度/0.15)` 防止用头或后背骗分；
`alive`/`imitation` 置 0；PPO `discounting = 0.993`。

### 结果：尚未学会（8 轮全部失败）

| 运行 | 起始分布 | 初始化 | 末期回报 |
| --- | --- | --- | --- |
| v1 scratch / standing | 70% 完全倒地 | 站立关节 / 从行走策略 | −2819 / −2865 |
| v3 tilt scratch / standing | 70% 倒地 + 30% 轻倾斜 | 随机关节 | −2819 / −2865 |
| easy lean | 仅 20–40° 轻倾斜 | 站立关节 | −3113 |
| s1 lean fine-tune | 仅轻倾斜，从行走策略恢复 | 站立关节 | −3195 |
| s2 mixed fine-tune | 35% 倒地 + 轻倾斜 | 随机关节 | −3147 |

诊断脚本全部归档在 `diagnostics/`，三个关键测量：

1. **奖励参照**（`diag_reward2.py`）：站立 home 姿态每步 **+2.34**（`up_z=0.934`），
   躺平约 **−3.34/步**。若策略只是停留在起始姿态就有约 +2300/回合，但所有运行都得到
   **−2900 至 −3200**，说明机器人**连轻微倾斜都站不住，直接倒下**。
   奖励设计本身没有问题。
2. **探索上限**（`test_feasibility.py`）：从倒地起始做 500 步均匀随机动作、12 个种子，
   最高 `up_z` 仅 **0.867**（且该种子起始就已是 0.873），达 0.85 以上者 1/12、
   达 0.50 以上者 6/12。即**从完全倒地出发，随机探索几乎不可能到达站立姿态**，
   站立奖励在概率上不可达。
3. **模型完整性**（`check_model_integrity.py`）：官方 `BEST_WALK_ONNX_2` 从 home
   姿态跑 10 秒，在原场景与新场景中结果**完全相同**（`final_up_z=0.9953`、
   `min_up_z=0.9937`、`final_height=0.1656`）。**加碰撞盒没有破坏物理**，
   getup 的失败是学习问题而非模型问题。

结论与下一步建议：

- **训练步数不是唯一问题**。getup 从 600 万步加到 **1 亿步**（100,925,440 步，
  两路：A 仅轻度倾斜、B 含 50% 完全倒地）后，**仍然 0/12 成功**：A 在完全倒地姿态下
  最高只到 `up_z=0.48`，B 最高只到 `0.51`。说明纯稀疏「站立奖励」在当前动作空间下
  无法从倒地姿态引导出起身动作，继续加步数的收益有限。
- 需要**参考动作或脚本化起身的模仿信号**提供中间引导（类似 `imitation` 对行走的作用）。
  这是与后退不同性质的问题：后退缺的是「正确的激励方向」（已修复），
  getup 缺的是「可达的中间路径」。
- 起始姿态需要更平缓的课程，并确认落点在落地时没有初始冲击。

## 为什么不直接部署最后一步

PPO 的最终 checkpoint 不一定最好。每轮训练都保存多个 Orbax checkpoint，分别导出 ONNX 后，用普通 MuJoCo 运行时比较：

- 实际持续时间；
- 局部前向速度和位移；
- `up_vector_z` 与跌倒标志；
- 不同速度和随机种子的稳定性。

候选模型只有在这些指标通过后才可以复制到控制目录。

## 未解决问题

- 后退策略尚未达到目标速度与稳定性的同时满足；
- 转向两个方向的响应幅度仍低于指令值（V2 约 55%–70%），需要高转速课程；
- 训练完成后仍需扩大到扰动、地面摩擦变化和多随机种子测试；
- 仿真策略还没有完成实体机器人上的舵机标定与安全限幅验证。

## 2026-09-23：从官方 ONNX 建立可续训检查点

读取当天真机调试记录后，训练验收加入了 50 Hz 位置控制、14 舵机顺序、IMU 安装方向、
工厂零位偏置和悬空测试要求。首先对旧 PPO 恢复点做原生 MuJoCo 验收：在 `0.1 m/s`
指令下实际仅约 `0.0042 m/s`，证明此前多轮“稳定但不走”主要来自错误的训练起点。

官方 `BEST_WALK_ONNX_2.onnx` 与 Brax PPO actor 恰好都是
`101-512-256-128-28` 的 SiLU MLP。新增 `scripts/import_onnx_actor_checkpoint.py`，将
官方 actor 权重和观测归一化统计导入 Orbax 检查点，同时保留 critic 作为续训初始化。
往返导出后，在 512 个随机观测上的最大动作误差为 `1.4e-6`。

低学习率、域随机化训练结果：

| 候选 | 训练步数 | 学习率 | 前进实速（命令 0.1） | 正/负转向（5 s, 命令 ±0.15） | 原生验收 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 官方往返 | 0 | — | 0.0287 m/s | +11.85° / -1.46° | 9/9，0 跌倒 |
| sim2real v1 | 5.08M | 1e-6 | 0.0922 m/s | +23.73° / -1.83° | 27/27，0 跌倒 |
| turn balance v2 | 5.08M | 5e-7 | **0.1101 m/s** | **+52.41° / -21.06°** | 27/27，0 跌倒 |
| turn balance v3 | 2.62M | 2e-7 | 0.1172 m/s | +45.02° / -10.79° | 27/27，0 跌倒 |

v2 是当前最佳仿真候选；v3 虽然正向更接近命令目标，但负向转向退化，所以不晋级。
详细哈希、服务器路径和汇总指标见 `results/sim2real_official_seed_20260923.json`，真机安全
边界见 `HARDWARE_VALIDATION_20260923.md`。模型仍只允许在悬空/吊架保护下分级验证，尚未
批准直接落地行走。

## 2026-09-23：正反转向不对称的四假设验证

以 turn balance v2 为固定候选，用原生 MuJoCo 进行 0.3/0.6/1.0/1.4 四档
滑动摩擦扫描，同时记录足底接触力、偏航力矩、左右载荷和电机目标斜率
截断。反向 5 s 转角随摩擦提高从 `-22.97°` 单调恶化到 `-2.33°`，
反向双脚接触占比由 94.4% 上升到 100%，而正向仍保持 `+46°`至 `+54°`。
因此足底黏滑/双脚锁死是当前最可疑的物理根因。

电机限速可基本排除为主因：标准摩擦下正向截断帧为 6.4%，反向只有
1.6%。策略站稳后左右载荷约为 57:43，确认存在稳定偏载，但本轮还没有
单独证明它与反向失败的因果关系。

奖励诊断将总奖励下限临时改为足够小的 `-1e6`，以暴露被截掉的信号。反向错向帧中
85.7% 本应得到负奖励，但原训练会将其截成 0；分析窗口中被抹掉的负奖励为
40.28，正向为 23.22。奖励截断不是物理起点，但是重要的训练放大因素。
完整方法、表格和下一轮方案见 `TURN_ASYMMETRY_DIAGNOSIS_20260923.md`。
