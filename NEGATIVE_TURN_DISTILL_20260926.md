# 2026-09-26：Open Duck Mini v2 负转向镜像蒸馏

## 目标与边界

在原始平地 XML（地面摩擦 `0.6`、双脚底 `1.0`）中，使 `-0.15 rad/s` 的 5 秒负转接近 `-42.97°`，并保留站立、直走、停车和正转。以下均为仿真测试；没有真机验证，不能直接驱动舵机。基础 v5 ONNX 保持不变，新模型是一个需要与 v5 一起运行的 132 KB 动作残差 ONNX，不是独立策略。

## 为什么采用镜像教师

此前负转参考腿部姿态与站立参考逐值相同，单纯取消负转模仿奖励后，独立负转仅 `-18.37°`。按 MJCF 关节轴镜像 v5 的正转观测和动作，则独立负转达到约 `-56.75°` 且未跌倒，说明当前仿真机体具备足够的负转能力，主要不足在于策略的负向动作。将原策略与镜像策略按 `0.13:0.87` 混合，仅对负转生效，在 20 种子连续测试中负转平均 `-43.30°`，范围 `-44.51°` 至 `-42.47°`，`180/180` 阶段无跌倒。这是训练教师，不是最终模型。

## 训练过程

1. 以 v5 为基础采集 24 个种子、4 种起转前缀下的 6000 条镜像教师观测和动作。
2. 训练 115→128→128→14 的残差网络；第一次离线误差虽小，但闭环在 5 个种子中过转到 `-46.96°` 至 `-62.36°`。
3. 将学生自身走出的状态重新交给教师标注，进行两轮 DAgger 式数据回流，每轮 24 个种子、6000 条数据。第三轮模型在未参与训练的种子 `100–119` 上平均负转 `-40.31°`，`180/180` 阶段无跌倒。
4. 在调参种子上用残差目标幅度 `1.00`、`1.02` 两端试验，分别约为 `-40.31°`、`-45.66°`；选中间的 `1.01`。该窄范围模型在全新种子 `200–219` 上平均 `-42.92°`，范围 `-43.54°` 至 `-42.16°`，`180/180` 阶段无跌倒；相同种子的原 v5 平均 `-28.98°`。但窄范围模型在 `-0.14/-0.16` 指令附近会突然退回旧策略，因此不作为最终候选。
5. 追加 48 个种子、`-0.13` 至 `-0.17 rad/s` 随机指令的 12000 条教师数据，合计 30000 条；重新训练宽范围残差。修正权重在 `-0.13` 至 `-0.17` 区间内为 1，区间外逐渐减小。代码保留了此前窄范围模式，测试时使用 `--negative-residual-band wide`。

## 宽范围候选的最终测试

独立起转均从标准站姿站立 3 秒开始，随后固定指令转 5 秒。`-0.13/-0.14/-0.15/-0.16/-0.17 rad/s` 的转角分别为 `-39.75°/-41.91°/-43.67°/-44.14°/-45.90°`，机身朝上指标在五组中最低为 `0.996`，没有观察到跌倒。原始逐步检查结果见 `results/negative_mirror_distill_v6_independent_sweep_up.json`。

连续测试使用新种子 `300–319`，各阶段依次为站立、慢走、停车、直走、停车、正转、停车、负转、停车；负转时长 5 秒。不同负转命令分别验证如下：

| 负转指令 | 5 秒理论目标 | 测得平均值（范围） | 完成阶段 / 跌倒 |
| --- | ---: | ---: | ---: |
| `-0.13 rad/s` | `-37.24°` | `-40.57°`（`-40.92°` 至 `-40.09°`） | `90/90` / `0` |
| `-0.15 rad/s` | `-42.97°` | `-44.14°`（`-44.68°` 至 `-43.58°`） | `180/180` / `0` |
| `-0.17 rad/s` | `-48.70°` | `-45.66°`（`-46.73°` 至 `-44.92°`） | `90/90` / `0` |

三个速度均能稳定负转，比原 v5 在目标 `-0.15` 时约 `-29°` 的负转明显改善；但两端仍有约 `3°` 的跟踪误差。对 `-0.10` 至 `-0.20` 的边界扫速还发现，尽管动作残差的权重是平滑变化的，实体动力学响应在 `-0.12/-0.18` 附近仍会变弱，不能把该候选称为全速度范围的负转策略。当前有数据支持的使用范围仅是固定 XML 中的 `-0.13` 至 `-0.17 rad/s`；未覆盖摩擦、地形、硬件误差或外力扰动。

## 保存位置与复现

- 基础策略：`/data/shijinsheng/open_duck/training/official_seed_turn_balance_v5_yaw_error/final.onnx`，SHA-256 `6f60d1d0a0d5e9f780e18a0c202c816c8d0183c855f78fa0a2580dce776d383d`。
- 宽范围残差：`/data/shijinsheng/open_duck/training/negative_mirror_distill_v6_model/residual.onnx`，SHA-256 `c39bb4ba156d01745b5d4a2103513953644ab168bd4b4c746f6159bf158df64c`。
- 数据采集与训练代码：`diagnostics/collect_negative_mirror_teacher.py`、`scripts/train_negative_residual.py`；运行时诊断适配器：`diagnostics/negative_turn_residual.py`；独立和连续评估：`diagnostics/diagnose_turn_four_hypotheses.py`、`validate_sim2real_policy.py`。
- 汇总和逐阶段原始 JSON 位于 `results/negative_mirror_distill_*.json` 与 `results/turn_v5_baseline_final_heldout20.json`。服务器保留训练数据、ONNX、PyTorch 权重及日志；GitHub 提交代码和评估结果，不将实验性二进制模型误标为真机可用版本。

后续若要扩大控制范围，应重新采样并验证更宽的负转指令，且为控制接口设置明确的速度限幅和失稳保护；真机前还需独立核对关节方向、舵机限位、供电和急停。
