# 2026-09-23 真机约束与仿真候选

## 已确认的真机状态

- 树莓派运行官方 Open Duck Mini Runtime，策略 50 Hz、舵机位置控制；仿真物理频率 500 Hz。
- 14 个舵机均可通信和读取，关节顺序与策略 14 维动作一致。
- IMU 与左右足底接触传感器可用，配置保持 `imu_upside_down=true`。
- 保留已测工厂零位偏置；腿部高增益约为 30，头部约为 8。
- 低增益单电机测试中部分关节响应较弱，不能据此判断舵机损坏。
- 官方运行时已完成低增益、归位、高增益、手柄识别、参考轨迹加载和控制循环启动。
- 真机尚未确认地面行走；任何新模型都不得跳过悬空、吊架和急停测试直接落地。

## 对训练和验收的影响

1. 电机速度限制必须每个 50 Hz 控制周期应用一次，而不是在 500 Hz 物理子步中重复应用。
2. 验收序列包含站立、低速、停止、`0.1 m/s` 前进、正负 `0.15 rad/s` 转向及阶段间停车。
3. 候选必须在普通 MuJoCo 推理链路中完成全部阶段且无跌倒；训练回报不能代替行为验收。
4. `0.03 m/s` 在官方策略中几乎等于站立。首次悬空观察步态时，应在安全保护下短时使用约 `0.08–0.10 m/s`，但本文件不代表已批准真机部署。

## 当前结论

从历史 PPO 检查点恢复后，`0.1 m/s` 指令实际仅约 `0.0042 m/s`，说明该检查点已经退化为近似站立。继续从它训练不可取。

已新增官方 ONNX 到 Orbax PPO 检查点的转换流程。512 组随机观测比较中，转换往返模型与官方模型的最大动作误差为 `1.4e-6`。以这个检查点为起点的候选如下：

| 模型 | 前进实际速度 | 正向转弯/5 s | 负向转弯/5 s | 完成阶段 | 跌倒 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 官方往返基线 | 0.0287 m/s | +11.85° | -1.46° | 9/9 | 0 |
| sim2real v1 | 0.0922 m/s | +23.73° | -1.83° | 27/27 | 0 |
| turn balance v2 | **0.1101 m/s** | **+52.41°** | **-21.06°** | 27/27 | 0 |
| turn balance v3 | 0.1172 m/s | +45.02° | -10.79° | 27/27 | 0 |

`turn balance v2` 是当前仿真最佳候选；v3 的负向转弯退化，因此被拒绝。由于仍存在转向不对称、停车后残余位移以及仿真到真机差异，v2 尚未批准部署到真机。
